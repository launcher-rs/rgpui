# extended_components（扩展组件）

展示并入 rgpui 核心的扩展组件库（原 rgpui-ui）：`SplitPane` 分隔面板、
`TagInput` 标签输入、`TypeWriter` 打字机动画、`Waveform` 音频波形、
`Sparkline` 迷你走势图、`SVGRenderer`、弹簧动画与预设。

## 运行

```text
cargo run -p extended_components
```
