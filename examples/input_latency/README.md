# input_latency（输入延迟统计，`input-latency-histogram` feature 门控）

展示 `window.input_latency_snapshot()`：输入事件到帧呈现的延迟直方图、
每帧合并的输入事件数、绘制期间丢弃的事件计数。窗口内点击/悬停可产生数据。

## 运行

```text
cargo run -p input_latency
```
