# form（表单）

表单组件示例：`v_form` / `h_form`（`label_width`）、网格布局（`columns`）、
必填标记（`required`）、字段描述（`description`）、无标签缩进（`label_indent`），
以及与 `Input` / `NumberInput` / `TextArea` / `Select` / `Checkbox` /
`Switch` / `RadioGroup` / `Button` 的组合、提交校验与重置。

## 运行

```text
cargo run -p form
```
