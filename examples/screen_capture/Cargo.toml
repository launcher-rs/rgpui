[package]
name = "screen_capture"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
rgpui = { workspace = true, features = ["screen-capture"] }
rgpui-platform = { workspace = true, features = ["screen-capture"] }
image.workspace = true
