# desktop_pet（2D 桌面宠物）

`rgpui-character` 完整链路演示：`AssetManager` 注册 sprite 动画资源 →
`Behavior` 决定角色动作 → `CharacterRuntime` 推进行为/物理/动画 →
`img()` 把纹理绘制到鼠标穿透的透明覆盖窗口。

## 运行

```text
cargo run -p desktop_pet
```
