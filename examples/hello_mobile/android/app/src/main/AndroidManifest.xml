<?xml version="1.0" encoding="utf-8"?>
<!-- hello_mobile 最小 Android 宿主：NativeActivity 直载 Rust .so，无 Java 代码。 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 按需增减：网络 / 通知等权限在 M3 包生态接入时再加 -->
    <!-- <uses-permission android:name="android.permission.INTERNET" /> -->
    <!-- 振动器（普通权限，声明即授）：cx.vibrate 用 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:label="HelloMobile"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:resizeableActivity="true">
        <activity
            android:name="rs.rgpui.GpuiInputActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|keyboardHidden|screenSize|screenLayout|density|uiMode"
            android:screenOrientation="unspecified">
            <meta-data
                android:name="android.app.lib_name"
                android:value="hello_mobile" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
