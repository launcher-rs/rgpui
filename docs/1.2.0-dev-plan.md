# 1.2.0 开发文档

> 实现依据本文档；每项完成后勾选。顺序：布局 → 组件 → 示例 → 验证。
> 组件统一约定：中文文档注释、风格走 `Styled`/`Sizable` 等现有 trait、
> 注册进模块 `mod.rs` + `prelude`（如适用）、零 `#[allow(dead_code)]`。

## A. 布局整理

- [ ] A1. `system/` 目录：`tray.rs`、`focus_trap.rs`、`tab_stop.rs`、`window_border.rs`、
      `window_ext.rs`、`svg_renderer.rs`（私模块直搬；`svg_renderer` 内 `use super::*`
      改 `use crate::*`；根重导出改 `pub use system::<m>::*`）。
- [ ] A2. 公模块同名进目录（路径不变，无需别名）：`webview/`、`tokio/`、`i18n/`、
      `single_instance/`、`file_watcher/`、`config_store/`、`theme_watcher/`、
      `mouse_gestures.rs`→`gestures/`？（见注）、`scroll_physics.rs`、
      `component_text.rs`、`block_render.rs`、`virtual_scroll.rs`、`source_map.rs`、
      `markdown_ext.rs`、`window_positioner.rs`。
      注：`mouse_gestures`/`scroll_physics` 已在 `components/gestures` 语境出现，
      为避免混淆，目录沿用原名（`mouse_gestures/`、`scroll_physics/` 不存在则建同名目录）。
- [ ] A3. `chat_ui.rs` → `chat/mod.rs`，根加
      `#[deprecated(note = "已迁移到 rgpui::chat")] pub use chat as chat_ui;`。
- [ ] A4. `rgpui.rs` 按“框架核心 / 子系统 / 平台”分组注释。
- [ ] A5. `cargo check -p rgpui` + 全 workspace check 通过后提交。

## B. 缺口组件（全部进现有目录，不建新 crate）

- [ ] B1. **Sidebar**（新文件 `components/sidebar.rs`）：导航侧边栏。
      API Sketch：`Sidebar::new()` + `.item(id, label, icon)` + `.selected(id)` +
      `.on_select(handler)` + `.collapsible(bool)`（折叠仅图标栏）。
      验收：`rgpui_story` 或新示例可点、可折叠；clippy 干净。
- [ ] B2. **Select**（`menu/dropdown` 已有 `DropdownMenu`，补表单版 `form/select.rs`？——
      落点待定：若 `DropdownMenu` 已够用，则只做表单封装 `Select`（label + 值 + 校验集成）。
      先读 `menu/dropdown_menu.rs` 再定，文档此处更新结论）。
- [ ] B3. **Combobox**（可搜索单选/多选下拉）：`Input` + `PopupMenu` 组合，
      落 `input_ui/combobox.rs` 或 `components/combobox.rs`（看依赖方向定）。
- [ ] B4. **DatePicker**：日历弹窗 + 日期显示，落 `components/date_picker.rs`。
      最小可用：年月导航 + 日期格 + `on_select(NaiveDate)`（用 `chrono`，已是 workspace 依赖）。
- [ ] B5. **ColorPicker**：色相条 + 饱和度面板 + hex 输入，落 `components/color_picker.rs`，
      回调 `on_change(Hsla)`。
- [ ] B6. **Avatar**：头像（图片源）+ 字母回退 + 状态圆点，落 `elements/avatar.rs`。
- [ ] B7. **Tree**：树形组件（展开/折叠/缩进线/选中），落 `list/tree.rs`（与 `list/` 同目录）。
- [ ] B8. **Alert**：多变体提示消息（info/success/warning/danger），落 `elements/alert.rs`
     （与 `dialog/alert_dialog.rs` 区分：Alert 是行内提示条，不是弹窗）。

## C. 缺口示例

- [ ] C1. **dialog 示例**（`examples/dialog/`）：`Dialog` + `AlertDialog` 打开/确认/取消、
      `FocusTrapElement` 焦点锁定演示 + README。
- [ ] C2. **sidebar 演示**：并入 C1 或独立 `examples/sidebar/`？——定为独立示例，
      展示导航切换 + 折叠 + 与内容区联动 + README。

## D. 收尾验证（全部完成后）

- [ ] D1. `cargo check --workspace` / `cargo fmt --all` / 改动包 clippy / `cargo test --workspace`。
- [ ] D2. AGENTS.md 组件库索引表 + 完整性清单同步。
- [ ] D3. CHANGELOG 追加 1.2.0 节。
- [ ] D4. Single PR（版本分支已就是 `feat/1.2.0`，直接 `gh pr create`）。
