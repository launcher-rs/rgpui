# 1.2.0 开发文档

> 实现依据本文档；每项完成后勾选。顺序：布局 → 组件 → 示例 → 验证。
> 组件统一约定：中文文档注释、风格走 `Styled`/`Sizable` 等现有 trait、
> 注册进模块 `mod.rs` + `prelude`（如适用）、零 `#[allow(dead_code)]`。
> 组件 API 设计见 `1.2.0-design.md`；本文档由开发计划、搜索实战反馈、发布记录三份文档合并而成。

## A. 布局整理（已完成）

- [x] A1. `system/` 目录：`tray.rs`、`focus_trap.rs`、`tab_stop.rs`、`window_border.rs`、
      `window_ext.rs`、`svg_renderer.rs`（私模块直搬；另修 `tab_stop` 自引用）。
- [x] A2. 15 个公模块同名进目录（路径不变，零改动通过；`super::*` 全在 test mod 内，
      搬家语义不变）。
- [x] A3. `chat_ui.rs` → `chat/mod.rs` + deprecated 别名（1.3.0 删除；内部零引用旧路径）。
- [x] A4. `rgpui.rs` 三段式分组注释（框架核心 / UI 组件子系统 / 视图与窗口）。
- [x] A5. `cargo check -p rgpui` 通过后提交。

## B. 缺口组件（全部进现有目录，不建新 crate）

> 以 gpui-kit 为参照找缺口，但它不是上限：其他好用的组件（哪怕 gpui-kit 没有）
> 都可以加。下面的 B1–B8 是首批，后续随时追加。

- [x] B1. **Sidebar**（新文件 `components/sidebar.rs`）：导航侧边栏。
      API Sketch：`Sidebar::new()` + `.item(id, label, icon)` + `.selected(id)` +
      `.on_select(handler)` + `.collapsible(bool)`（折叠仅图标栏）。
      验收：`rgpui_story` 或新示例可点、可折叠；clippy 干净。
- [x] B2. **Select**（结论：`DropdownMenu` 是通用触发器+构造闭包，不适合简单值列表；
      新写 `components/select.rs`：选项 + 选中索引 + 占位符 + `on_change`，实例计数器防 ID 冲突）。
- [x] B3. **Combobox**（可搜索单选/多选下拉）：`Input` + `PopupMenu` 组合，
      落 `input_ui/combobox.rs` 或 `components/combobox.rs`（看依赖方向定）。
- [x] B4. **DatePicker**：日历弹窗 + 日期显示，落 `components/date_picker.rs`。
      最小可用：年月导航 + 日期格 + `on_select(NaiveDate)`（用 `chrono`，已是 workspace 依赖）。
- [x] B5. **ColorPicker**：色相条 + 饱和度面板 + hex 输入，落 `components/color_picker.rs`，
      回调 `on_change(Hsla)`。
- [x] B6. **Avatar**：头像（图片源）+ 字母回退 + 状态圆点，落 `elements/avatar.rs`。
- [x] B7. **Tree**：已存在（`tree.rs`：`TreeItem`/`TreeState`/`Tree` + `init`），无需新写；
      补 `dialog`/`sidebar` 示例时已验证可用。
- [x] B8. **Alert**：多变体提示消息（info/success/warning/danger），落 `elements/alert.rs`
      （与 `dialog/alert_dialog.rs` 区分：Alert 是行内提示条，不是弹窗）。

## B+. 灵感批次（设计见 `1.2.0-design.md`）

- [x] B9. **小件九连**：`Breadcrumb`、`Pagination`、`Steps`、`Timeline`、`Rate`、
      `Popconfirm`、`Toggle`、`Card`、`Typography`（纯 RenderOnce，每件 50–150 行）。
- [x] B10. **AI-chat 七件**（对标 AntDX，落 `chat/`）：`Prompts`、`Suggestion`、
      `ThoughtChain`、`Attachments` + `FileCard`、`Sources`、`Actions`、`Sender`。

## C. 缺口示例

- [x] C1. **dialog 示例**（`examples/dialog/`）：`Dialog` + `AlertDialog` 打开/确认/取消、
      `FocusTrapElement` 焦点锁定演示 + README。
- [x] C2. **sidebar 演示**（`examples/sidebar/` 独立示例）：导航切换 + 折叠 + 与内容区联动
      + README（兼作 `Conversations` 用法示范）。

## D. 收尾验证（全部完成后）

- [x] D1. `cargo check --workspace` / `cargo fmt --all` / 改动包 clippy / `cargo test --workspace`。
- [x] D2. AGENTS.md 组件库索引表 + 完整性清单同步。
- [x] D3. CHANGELOG 追加 1.2.0 节。
- [ ] D4. 版本分支收尾时 single PR 合入 main（Squash）。

## E. 搜索实战反馈 6 项（已完成，#14）

> 来源：ru_editor 搜索边栏实战（`Ctrl+F` 实时搜索 + 匹配高亮 + 右侧边栏），rgpui 1.1.1。

| # | 需求 | 落点 | 状态 |
|---|------|------|------|
| 1 | 多区间高亮/装饰 API | `TextDecoration` + `TextDecorationCollection` 公开导出（`input_ui/mod.rs`）+ 搜索 recipe 文档 | ✅ |
| 2 | 只读滚动 API | `InputState::reveal_offset` / `reveal_range`（`input_ui/state.rs`，不改光标选区） | ✅ |
| 3 | SearchPanel 可嵌入 | 新增 `SearchPanelState` 实体（`Render`），修空 `source` 祖传 bug；老 RenderOnce 版已删除（G4），新版补 `on_close` + `Styled` | ✅ |
| 4 | 订阅重入 panic | `subscribe` / `subscribe_in` doc 加重入警告 + `double_lease_panic` 追加订阅提示 | ✅ |
| 5 | 防抖原语 | `util::debounce::Debouncer`（generation 计数防竞态，虚拟时钟单测 2 通过）+ `App::debounce` 方法版（G5） | ✅ |
| 6 | 自动滚动补文档 | `set_selected_range` / `move_to` doc 追加自动滚动语义 + `reveal_*` 指引 | ✅ |

## F. 发布检查

- `cargo check --workspace` / `cargo fmt --all` / 相关包 clippy / `cargo test --workspace`
- AGENTS.md 完整性清单全过；组件库索引表同步新增组件
- CHANGELOG 追加 1.2.0 节；`cargo publish --dry-run --registry crates-io` 通过

## G. 原非目标转正（1.2 全部完成，设计见 `1.2.0-design.md` §G）

- [x] G1. **Dock 布局**：`DockAreaState` 实体 + 四方位面板 + 同组标签排序 + 布局 JSON 持久化。
- [x] G2. **Editor**：不建新组件，input `CodeEditor` 复用 + `tree-sitter` 可选 feature
     （默认关、wasm 禁用）实现 `Highlighter` + fold 数据源，先通 Rust 单语言。
- [x] G3. **Sidebar 增强**：section 分组头 + 可折叠分组 + `badge` 计数（拖拽排序不做）。
- [x] G4. **SearchPanel 老版删除**：删 RenderOnce 版约 370 行，新版补 `on_close` + `Styled`。
- [x] G5. **`cx.debounce` 方法版**：`App` 全局注册表 + key 隔离，一行调用。
- [x] G6. **Upload**：拖拽区 + 文件列表 + 进度，对接 `prompt_for_paths`；Web 降级。
- [x] G7. **Mermaid 子集**：flowchart LR/TB/RL/BT（矩形/圆角/菱形/圆形/直线箭头/边标签），零新依赖。
- [x] G8. **Carousel**：index + autoplay + loop + 指示器 + 滑动手势。
- [x] G9. **聊天三件**：`Bubble` + `MessageScroller`（吸底/新消息浮标）+ `Marker`
     （时间/未读分隔线）。

## H. Input 右键菜单（搜索标记的后续，设计见 `1.2.0-design.md` §H）

- [x] H1. **默认菜单**：`input_ui/context_menu.rs` 新建（`InputContextMenuBuilder` +
      `build_default_context_menu` + 状态查询 `has_selection/is_empty/is_disabled/can_undo/can_redo`），
      剪切/复制/粘贴/全选/撤销/重做按状态自动禁用；`InputState::render` 包 `.context_menu`
     （单行/多行/CodeEditor 全覆盖，裸 `Entity<InputState>` 同样生效）。
- [x] H2. **三档定制**：`Input::show_context_menu` 总开关 +
      `context_menu_extra` 追加（默认保留）+ `context_menu_override` 接管
      （可调 `build_default_context_menu` 拼回默认）；`InputState` 层同名 builder/`set_*`/`clear`；
      `Input` 层写入共享 state（粘性）。`mod.rs` 导出 `InputContextMenuBuilder`。
- [x] H3. **测试**：`context_menu.rs` 内 3 个 `#[rgpui::test]`
     （默认菜单跟随状态 / 追加+接管触发与清除 / 元素层写入 state）；改动包 clippy 干净。
- [x] H4. **文档**：本计划 §H + 设计文档 §H（含默认菜单禁用表与三档 recipe）。
- [x] H5. **演示**：`v1_2_showcase --bin editor` 内右键追加行（`context_menu_extra`
      转大写；编辑器内右键=默认菜单开箱即用；原独立 `context_menu` 演示页已并入，
      接管档见设计 §H recipe）。

## I. 行操作命令（设计见 `1.2.0-design.md` §I）

- [x] I1. **六命令**：`input_ui/line_ops.rs` 新建（`CopyLine` 复制行 /
      `DeleteLine` 删除行 / `MoveLineUp/Down` 移行 / `ToggleLineComment` 注释切换 /
      `JoinLines` 合并行），action 注册 + `Input` 上下文默认键位 + `Input` 外层
      `on_action` 接线；`line_comment_prefix` 可配（默认 `//`）。
- [x] I2. **测试**：6 个 `#[rgpui::test]`（复制/删除/移行往返/边界无操作/
      注释往返跳空行/合并清空白）；改动包 clippy 干净。
- [x] I3. **演示**：`editor` 页追加行操作键位，手动可验证。

## J. 键入体验四件（设计见 `1.2.0-design.md` §J）

- [x] J1. **自动闭合**：`auto_close.rs` 新建（环绕/跳过/补对/成对退格），
      trait 入口拦截 + raw 直通，默认多行开单行关；7 个测试。
- [x] J2. **只读模式**：`read_only` 字段 + builder/setter + `Input::read_only`
      透传；用户动作逐点拦截、API 照常写；菜单剪切粘贴撤销重做自动禁用。
- [x] J3. **电缩进**：`enter` 内 `{` 加级、`{}` 间拆三行；**括号匹配高亮**：
      `bracket_match.rs` 新建（纯扫描 + 装饰集合 + 三处刷新点），accent 底色；
      3 + 2 个测试；改动包 clippy 干净。

## K. 导航阅读三件（设计见 `1.2.0-design.md` §K）

- [x] K1. **当前行高亮**：`current_line.rs` 新建（独立装饰集合 + 三处刷新点），
      默认开、仅多行；1 个集成测试。
- [x] K2. **符号大纲**：`Highlighter::document_symbols`（默认空）+
      tree-sitter 实现（函数/结构体/枚举/Trait/impl/模块/常量/静态，按位排序）+
      `InputState::document_symbols/goto_symbol`；1 个门控测试。
- [x] K3. **查找接线**：`SearchPanelState::attach_editor`（文本同步 + 自动标黄 +
      默认跳转，不覆盖自定义导航）+ `set_highlight_colors`/`focus_search_input`/
      `set_on_navigate`；1 个集成测试；`editor` 演示页右列面板接编辑器实体
      （原独立 `search` 演示页已并入，替换接线一并迁移）。
- [x] K4. **约束记录**：非 tree-sitter 后端大纲为空；装饰只盖文本区；
      替换执行权留外部；改动包 clippy 干净。

## L. 多光标多选区（设计见 `1.2.0-design.md` §L）

- [x] L1. **模型**：`extra_selections`（主选区不动）+ 归一化 +
      `AddCursorAbove/Below` + 键位 + `Input` 接线；坍缩点：`move_to` 原子、
      鼠标左右键、Esc、全选、行操作、IME。
- [x] L2. **扇出编辑**：键入/退格/删除/剪切/复制/粘贴/回车 + `regroup_last`
      并组撤销；3 个测试（扇出键入 + 单次撤销整体回退 / 坍缩 / 渲染冒烟）。
- [x] L3. **渲染**：选区 path 向量化 + 额外光标 quad（同闪烁，主光标驱动滚动）；
      `editor` 页追加多光标键位；改动包 clippy 干净。

## M. 收尾规划转正（设计见 `1.2.0-design.md` §M；顺序 M1 先行，M2→M3→M4 串行，M5/M6/M7 可并行）

> 落点约定（分拆后）：编辑器功能一律进 `input_ui/editor/` + `EditorState` 方法，
> 表单 `input/` 不动，共享走 `TextCore`。非目标已清零：原 §N 四项全部转正为 §O。

- [x] M1. **P3 分拆收尾**：分拆设计 §4/§5 按 P3a 实际路线更新（包装实体，非 `TextCore` 直连）+
      `EditorState` 差集盘点并补透传（`set_highlighter`/`document_symbols`/`goto_symbol` 等，
      以盘点为准）+ 12 消费方零改动复查 + 3.2 未落地件砍掉并如实记录 +
      `input/state.rs` 红线冻结（新功能禁入、只修 bug）；全绿 + IME 手测 + clippy。
- [x] M2. **LSP 编辑器侧接线**：`editor/lsp_attach.rs` 新建
     （`set_completion/diagnostics/hover_provider` + `debounce` 触发 +
      补全弹窗/诊断装饰/hover 浮层）；`editor` 演示页接本地假 provider（静态词表 +
      固定诊断）；改动包 clippy 干净。
- [x] M3. **snippets 最小版**：`editor/snippets.rs` 新建（`expand_snippet` 解析
      `$1/$2/$0` + `${1:占位}` + `next_placeholder` 跳转/退出 + 补全联动）；
      单测（展开/跳转往返/退出/联动 + multicursor/undo/IME 交互注明）。
- [x] M4. **inlay hints**：`editor/inlay_hints.rs` 新建（`InlayProvider` 默认空 +
      开关默认关 + 行尾虚拟文本三不：不占布局/不进 Rope/不参与选中复制）；
      大文档只渲染可视行。
- [x] M5. **sticky scroll**：`editor/sticky_scroll.rs` 新建（大纲栈 overlay +
      点击走 `goto_symbol`，默认开、仅 `Editor`）；无大纲时不显示不报错。
- [x] M6. **多语言注册表**：`highlight/mod.rs` 注册表（`register_highlighter`/
      `highlighter_for`，未注册静默降级）+ `EditorState::set_language`
      （查表 + 大纲联动，wasm 直接降级）；加新语言三步文档；不加新 grammar。
- [x] M7. **快捷键用户层**：keymap 文件加载（`load_keymap_json`，未知 action 报错，
      `"action": null` = `NoAction`）+ 设置页 recipe（`HotkeyInput` 录制 +
      `bindings_for_action` 回显 + 冲突提示）+ 示例页。
- [x] M8. **文档同步**：AGENTS.md 索引/清单 + CHANGELOG 1.2.0 节补 M/O 条目 + D4 single PR
      （随版本分支收尾）。

## O. 第二批转正（原 §N 非目标，复查无一落地，全部纳入 1.2；设计见 `1.2.0-design.md` §O）

> §N 已删除。复查结论：`lsp/` 只有 `LspClient` trait 无传输实现；无
> minimap/vim/codelens/标尺/括号彩虹代码（vim 仅 keymap 上下文谓词解析）；
> 高亮只有 tree-sitter-rust；无 `extensions`/`on_edit` 表。
> 顺序：O7 先行（分拆件收尾，M1 结算口径）→ O1/O6（传输/语言包，接 M2/M6）→
> O2–O5（视图件）可并行。

- [x] O1. **stdio LSP 传输/进程管理**：`lsp/stdio.rs`（或 `lsp/process.rs`）新建
      （子进程 spawn + JSON-RPC Content-Length 收发 + 请求 id 路由 + 通知分发 +
      `StdioLspClient: LspClient` + stderr 日志透出）。只做 stdio（TCP/websocket 不做）；
      进程异常退出则在途请求全失败、不守护重启（文档注明）；与 M2 provider 对接，
      演示页可拉 rust-analyzer 手验；传输层单测走内存 duplex 假服务端。改动包 clippy 干净。
- [x] O2. **Minimap**：`input_ui/editor/minimap.rs` 新建（缩略文本块 + 可视区高亮 +
      点击/拖动跳转 + 开关，默认关）。约束：纯 overlay（三不：不占布局/不进 Rope/
      不参与选中，与 inlay 同理）；大文档抽样渲染（每 N 行取 1，N 可配）；
      跳转走现有 `reveal_offset`/`goto_symbol`；`editor` 演示页可开关手验。1 个渲染冒烟单测。
- [x] O3. **Vim 模式最小可用**：`input_ui/vim.rs`（或 `editor/vim.rs`，看对表单复用情况定）新建
      （normal/insert/visual 三态 + 移动 `hjkl/wb/0/$/gg/G` + 编辑 `i/a/o/x/dd/yy/p/u` +
      `vim_mode` 状态进 keymap 上下文谓词（解析已支持）+ 状态行模式指示 +
      `EditorState` 开关透传，默认关）。`:` 命令、宏、寄存器、插件、可视块不做；
      支持矩阵文档注明（缺什么一眼可见）。单测覆盖模式切换与核心操作；改动包 clippy 干净。
- [x] O4. **CodeLens**：`input_ui/editor/codelens.rs` 新建（`CodeLensProvider` trait +
      行上透镜行：引用数/测试运行等展示 + 点击回调；数据源 M2 同款注入，无 provider 零开销）。
      v1 只做单行文本透镜（多行折叠/参数透镜不做）；行高固定、不挤占编辑区滚动
      （overlay 层，sticky 同款）；演示页假 provider 手验 + 1 个单测。
- [x] O5. **标尺 + 括号彩虹**：标尺（指定列竖线 + 可配列数/颜色，默认关）；
      括号彩虹（`bracket_match.rs` 扩展：嵌套层级按调色轮着色 + 开关，默认关，
      与现有匹配高亮共存规则文档注明）。落点与现有装饰同池；各 1 个单测。
- [x] O6. **首批非 Rust grammar**：按 M6 三步机制加 JSON + TOML
      （配置场景刚需、语法小、查询易写；各独立 feature 门）+ 高亮 query +
      `document_symbols`（对象键/表头）+ 注册一行。验收：`set_language("json"/"toml")`
      可用 + 大纲非空单测；JS/TS 等大语法不进 1.2（留扩展点）。
- [x] O7. **分拆 3.2 未落地件**：`extensions` 扩展表 + `on_edit` 文本变更订阅表
      （M1 按"过度设计"砍掉；现 M2–M6/O1–O6 落地后挂变更与扩展点的功能已超 10 个，
      总线有真实需求，按分拆文档 3.2 机制思想实现）。keymap `vim` 命名空间预留随 O3 恢复。
      注册+触发单测；分拆设计文档 3.2 同步更新为"已落地"；`input/state.rs` 红线不变
      （扩展走新表，不进 state）。
