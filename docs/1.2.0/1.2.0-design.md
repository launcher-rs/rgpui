# 1.2.0 组件设计文档

> 灵感来源：gpui-kit（37 组件目录）为参照，shadcn、Ant Design（含 AntDX AI 组件）
> 找好用的。gpui-kit 没有但好用的也加。全部直接进 `rgpui` 现有目录，不建新 crate。
> 约定：中文文档注释；builder 风格；`Styled`/`Sizable` 等现有 trait；中文注释覆盖所有公开项。
> 开发计划与验收见 `1.2.0-dev-plan.md`；原非目标已全部转正为目标（§G + §O），本文档无非目标章节。

## B1. Sidebar（`components/sidebar.rs`，新建）

导航侧边栏（对标 shadcn Sidebar / AntDX Conversations 承载体）。

```rust
pub struct Sidebar { items: Vec<SidebarItem>, selected: Option<SharedString>, collapsible: bool, collapsed: bool, on_select: Option<Rc<...>> }
pub struct SidebarItem { id: SharedString, label: SharedString, icon: Option<IconName> }

impl Sidebar {
    pub fn new() -> Self;
    pub fn item(mut self, id: impl Into<SharedString>, label: impl Into<SharedString>, icon: Option<IconName>) -> Self;
    pub fn selected(mut self, id: impl Into<SharedString>) -> Self;
    pub fn collapsible(mut self, collapsible: bool) -> Self;
    pub fn on_select(mut self, handler: impl Fn(&SharedString, &mut Window, &mut App) + 'static) -> Self;
}
// RenderOnce 即可（状态由父持有），selected 变化父 cx.notify()
```

## B2. Select（已定：新写 `components/select.rs`）

`DropdownMenu` 是通用触发器 + 构造闭包，不适合简单值列表，故新写：
选项 + 选中索引 + 占位符 + `on_change`，实例计数器防 ID 冲突。

## B3. Combobox（`components/combobox.rs`，新建）

可搜索单选/多选下拉（对标 shadcn Combobox）：`Input` 过滤 + `PopupMenu` 列表。

```rust
pub struct Combobox { input: Entity<InputState>, items: Vec<SharedString>, filtered: Vec<usize>, multiple: bool, selected: Vec<usize>, on_change: ... }
// 需要 Entity（过滤状态 + 订阅输入变更）→ 实现 Render，父 cx.new 持有。参考 SearchPanelState 模式。
```

## B4. DatePicker（`components/date_picker.rs`，新建）

日历弹窗 + 日期显示。最小可用：年月导航 + 日期格 + `on_select(NaiveDate)`。
`chrono` 已是 workspace 依赖。月份网格纯 div 渲染，弹窗复用 `Popover`。

## B5. ColorPicker（`components/color_picker.rs`，新建）

色相条 + 饱和度明度面板（canvas 或 div 渐变近似）+ hex 输入框，回 `on_change(Hsla)`。
精度要求不高（UI 取色），canvas 自绘可接受。

## B6. Avatar（`elements/avatar.rs`，新建）

头像：图片源（`img` 复用）+ 无图时首字母回退 + 右下状态圆点（online/offline/busy）。
纯 RenderOnce div 组合，无状态。

## B7. Tree（`list/tree.rs`，新建，与 `list/` 同目录）

树形组件：缩进线、展开/折叠三角、选中高亮。
`TreeNode { id, label, children, expanded }` + `TreeState`（展开集合 + 选中）。
虚拟化先不做（节点数 < 1k 直接渲染；大数据走 VirtualList 自行组装，文档注明）。

## B8. Alert（`elements/alert.rs`，新建）

行内提示条（对标 AntD Alert）：info/success/warning/danger 四变体 + 图标 + 可关闭。
与 `dialog/alert_dialog.rs` 区分（那是模态弹窗）。

## B9. 小件九连（shadcn/AntD 共有，rgpui 缺的）

全是纯 RenderOnce div 组合，无状态，单个文件 50–150 行：
`Breadcrumb`（路径导航，`elements/breadcrumb.rs`）、`Pagination`（页码导航，
`components/pagination.rs`，`on_change(usize)`）、`Steps`（步骤条，
`components/steps.rs`）、`Timeline`（时间线，`components/timeline.rs`）、
`Rate`（星级评分，`components/rate.rs`，`on_change(u8)`）、`Popconfirm`
（气泡确认，`menu/popconfirm.rs`，复用 `Popover` + 双按钮）、`Toggle`/`ToggleGroup`
（切换按钮，`elements/toggle.rs`）、`Card`（卡片容器 header/content/footer，
`elements/card.rs`）、`Typography`（`Title`/`Text`/`Link` 排印辅助，`elements/typography.rs`）。

## B10. AI-chat 件（对标 AntDX，落 `chat/` 目录——A3 搬迁后）

`chat_ui.rs` 已有 Message/MessageGroup；补：
`Prompts`（提示词芯片组，`on_pick`）、`Suggestion`（追问建议行）、
`ThoughtChain`（思维链步骤折叠列表）、`Attachments` + `FileCard`（附件列表）、
`Sources`（引用来源 chips）、`Actions`（消息操作行：复制/赞/踩）、`Sender`
（输入框 + 发送按钮组合，包 `Input` + 回车发送）。
`Conversations` 不做新组件（= Sidebar 用法，文档注明）。

## 落点总表

| 组件 | 文件 | 需 Entity |
|------|------|-----------|
| Sidebar/Alert/Avatar/Breadcrumb/Card/Typography/Toggle | `components/sidebar.rs`、`elements/alert.rs`、`elements/avatar.rs`、`elements/breadcrumb.rs`、`elements/card.rs`、`elements/typography.rs`、`elements/toggle.rs` | 否 |
| Select/Combobox/DatePicker/ColorPicker/Tree/Pagination/Steps/Timeline/Rate/Popconfirm | `components/*.rs`（combobox/tree 见上） | Combobox/Tree/DatePicker/ColorPicker 是，其余否 |
| AI-chat 七件 | `chat/*.rs` | Sender/ThoughtChain 视交互定，其余否 |

## G1. Dock 布局（`components/dock.rs`，新建）

**原因**：VS Code / Zed 级 IDE 都靠 Dock 组织工作区（文件树 + 编辑器 + 终端 + 问题面板），
ru_editor 这类应用现在只能手写 `v_flex`/`h_flex` 硬编码布局，面板拖不动、关了恢复不了。
**好处**：应用只需声明“有哪些面板”，拖拽停靠/标签切换/布局持久化框架全包，IDE 类应用开箱即用。

```rust
pub enum DockPosition { Left, Right, Bottom, Center }
pub struct DockPanel { id: SharedString, title: SharedString, position: DockPosition, content: AnyElement, closable: bool }
pub struct DockAreaState { panels: Vec<DockPanel>, sizes: ..., active_tabs: ... }
// 状态实体（Render），面板拖拽经现有 Draggable/DropZone + TabDragState 实现同组标签排序；
// 布局序列化：positions + sizes + active_tab 存 JSON，启动时恢复。
```

## G2. Editor（不建新组件，input `CodeEditor` + tree-sitter 后端）

**原因**：`input_ui` 的 `CodeEditor` 外壳已完备（行号/折叠图标 UI/缩进线/display_map 投影层），
空的只是两个数据源——fold 候选（`folding.rs`  stub 返回空）与高亮实现（`Highlighter` trait 无实现）。
另起 editor 组件会分叉第二套文本状态（选区/滚动/编辑），长期维护两套必烂。
**好处**：复用现有编辑行为零回归；tree-sitter 只做语法分析后端，经 `Highlighter` trait 注入，
wasm 下自动降级 `NoHighlightStyles`；语言按子 feature 拆分，不用的语言不编译。

```rust
// crates/rgpui/Cargo.toml
tree-sitter = ["dep:tree-sitter-rust"]  # 默认关，#[cfg(all(not(target_family = "wasm"), feature = "tree-sitter"))]
pub struct TreeSitterHighlighter { ... } // impl Highlighter（update/styles/fold_ranges_for_edit）
impl InputState { pub fn set_highlighter(&mut self, h: Arc<dyn Highlighter>, ...) } // 接 display_map
```

## G3. Sidebar 增强（`components/sidebar.rs` 加三件）

**原因**：现有 Sidebar 只是“应用导航栏”（id/label/icon + 折叠），IDE 侧边栏的分组、
未读数、折叠分组都没有，ru_editor 切过去不够用。
**好处**：补 `section` 分组头 + 可折叠分组 + `badge` 计数即达 IDE 基线；拖拽排序涉及手势大改，1.2 不做。

## G4. SearchPanel 老版删除（`components/search_panel.rs` 删 370 行）

**原因**：老 `SearchPanel`（RenderOnce）构造要 `&mut Context<Self>` 却无法被父长期持有，
实质不可嵌入；且回调全是空实现（替换按钮误调 next_match）。零调用方，留着只会误导新人。
**好处**：删后新版 `SearchPanelState` 补 `on_close` + `Styled` 两行 builder 即 100% 平替，
API  surface 减半，维护只剩一条路。

## G5. `cx.debounce` 方法版（`App`/`Context` 便捷方法）

**原因**：`Debouncer` 结构版要求调用方自持实例 + 自传 executor，搜索框这种“每次输入都防抖”
的高频场景样板代码多。方法版把实例挂 `App` 全局注册表，一行调用。
**好处**：`cx.debounce("search", duration, callback)` 一行搞定，key 隔离多路防抖；
结构版保留给需要独立生命周期的场景。

## G6. Upload（`components/upload.rs`，新建）

**原因**：平台原生文件对话框三桌面端全有（`prompt_for_paths`），缺的只是 UI 层——
拖拽区 + 文件列表 + 进度，现在每个应用都要手写一遍。
**好处**：纯 UI 组件 + 现有平台对话框对接，桌面端零新增平台代码；Web 明确降级
（`<input type=file>` 或纯展示），不阻塞。

## G7. Mermaid 子集（`components/mermaid.rs`，新建，flowchart LR/RL）

**原因**：文档/ COT 思维链（ThoughtChain）经常要画流程图，现在只能贴静态图。
完整 Mermaid 无依赖可用，但流程图 80% 场景 = 矩形/菱形 + 直线箭头 + 文本。
**好处**：手写子集解析（`A-->B`、`A{判断}`、`A[矩形]`）+ 现有 quad/line 原语布局渲染，
零新依赖；完整语义（子图/曲线边）明确不做，文档写清边界。

## G8. Carousel（`components/carousel.rs`，新建）

**原因**：展示型应用（story/官网/引导页）轮播是刚需，现在只能手写定时器切索引，
指示器/手势/循环样板每个应用重复写。
**好处**：`index + autoplay + loop + 指示器 + 滑动手势` 一次封装（约 300 行，
动画/滚动地基现成），展示类示例直接复用。

## G9. 聊天三件（`chat/`：Bubble / MessageScroller / Marker）

**原因**：B10 七件补的是“消息8462”，但聊天的“容器层”还是裸的——`ChatView` 只是
`v_flex` 裸文本，无气泡样式、无滚动吸底、无时间/未读分隔线，AI 对话应用撑不起来。
**好处**：`Bubble`（左右气泡 + 头像 + 圆角）+ `MessageScroller`（吸底 + 新消息浮标，
包一层滚动容器）+ `Marker`（时间/未读分隔线），ChatView 升级为可直接落地的对话容器。

## H. Input 右键菜单（`input_ui/context_menu.rs`，新建 + `state.rs`/`input.rs` 改三处）

**原因**：搜索标记（`TextDecorationCollection` + `SearchHighlight`）补齐了“读”，
但 input 右键只移动光标、不弹菜单——剪切/复制/粘贴全靠背快捷键，新用户上来就懵。
**好处**：默认菜单开箱即用；三档定制由简到繁，与 `Tree::context_menu` 同模式
（builder 存 state、渲染时包 `.context_menu`），学一处会两处。

默认菜单（按状态自动禁用，不可点但仍展示）：

| 菜单项 | 动作 | 禁用条件 |
|--------|------|----------|
| 剪切 | `Cut` | 无选区 / 输入框禁用 |
| 复制 | `Copy` | 无选区 |
| 粘贴 | `Paste` | 输入框禁用 |
| 全选 | `SelectAll` | 文本为空 |
| 撤销 | `Undo` | 无撤销栈 / 输入框禁用 |
| 重做 | `Redo` | 无重做栈 / 输入框禁用 |

```rust
pub type InputContextMenuBuilder =
    Rc<dyn Fn(PopupMenu, Entity<InputState>, &mut Window, &mut App) -> PopupMenu>;

// Input 元素层（三档，写入共享 InputState，粘性，后设置的生效）
Input::new(&state).show_context_menu(false);              // 1. 总开关
Input::new(&state).context_menu_extra(|menu, _, _, _| menu);    // 2. 追加（默认保留，自定义跟分隔符后）
Input::new(&state).context_menu_override(|menu, state, _, cx| { // 3. 接管（默认不要）
    let menu = InputState::build_default_context_menu(menu, &state, cx);
    menu.separator().menu("我的动作", Box::new(MyAction))
});
// InputState 层同名方法（builder 版创建时用 + set_* 版创建后改 + clear 恢复默认）
impl InputState {
    pub fn build_default_context_menu(menu: PopupMenu, state: &Entity<InputState>, cx: &App) -> PopupMenu;
    pub fn has_selection(&self) -> bool;  // 另有 is_empty / is_disabled / can_undo / can_redo
}
```

约束：菜单挂在 `InputState::render` 的文本区上（单行/多行/CodeEditor 全覆盖，
裸 `Entity<InputState>` 直渲染同样生效；前缀/后缀装饰区不触发）；
动作用 `input` 命名空间原生 action（`Copy`/`Cut`/`Paste`/`SelectAll`/`Undo`/`Redo`），
经触发器祖先链冒泡到 `Input` 外层已注册的 `on_action`，无需额外接线。

## I. 行操作命令（`input_ui/line_ops.rs`，新建 + action/键位/`Input` 接线）

**原因**：编辑器“写”的第一体感缺口——VS Code 用户肌肉记忆的行操作一个没有，
只能整行选中再剪切粘贴。
**好处**：六个小命令一次补齐，全走 `replace_text_in_range_silent`（历史/装饰/
高亮联动免费），`editor` 演示页可直接手动验证。

```rust
actions!(input, [CopyLine, DeleteLine, MoveLineUp, MoveLineDown, ToggleLineComment, JoinLines]);
// 默认键位（Input 上下文）：shift-alt-down 复制行；cmd/ctrl-shift-k 删除行；
// alt-up/down 移行；cmd/ctrl-/ 注释切换；cmd/ctrl-j 合并行。
impl InputState {
    pub fn line_comment_prefix(mut self, prefix: impl Into<SharedString>) -> Self; // 默认 "//"
}
```

语义（对标 VS Code）：目标行 = 无选区取光标行，有选区取覆盖行（结尾恰在行首
则不含该行，末尾幻影空行不计入）；复制行光标跟到复刻行同列；删除行光标落替补
行行首；移行到边界无操作；注释切换全已注释则解（空行跳过，符后带一空格），
否则加；合并行清两侧空白、单空格连接。单行模式/禁用时传播动作不执行。

## J. 键入体验四件（`input_ui/auto_close.rs` + `bracket_match.rs` + 只读/电缩进）

**原因**：编辑器“写”的第二体感缺口——敲 `(` 不补 `)`、展示文本只能用变灰的
`disabled`、回车不懂 `{}`、光标旁括号无匹配提示。
**好处**：四件全走现有管线（装饰集合/替换收尾/主题），对外只有 builder 开关。

```rust
// 1. 括号自动闭合（默认多行开、单行关，显式可覆盖；尖括号 excluded）
InputState::new(window, cx).multi_line(true).auto_close_pairs(true);
// 规则：有选区 + 左括号 → 环绕；右括号 + 光标后同符 → 跳过；
// 左括号 + 光标后非单词字符 → 补对光标留中间；退格夹空对 → 成对删。
// 2. 只读模式（正常样式，可选可复制；程序化写入不受影响）
Input::new(&state).read_only(true); // 菜单的剪切/粘贴/撤销/重做自动禁用
// 3. 电缩进：`{` 后回车多缩一级，`{}` 间回车拆三行光标落中
// 4. 括号匹配高亮：accent 底色，move/select/替换收尾自动刷新
InputState::new(window, cx).bracket_match(true); // 默认开，仅多行
```

约束：键入拦截点是 `EntityInputHandler::replace_text_in_range`（OS 入口），
内部实现拆出 `replace_text_in_range_raw` 专供程序化调用——API（`insert`/
`replace`/`set_value`）与用户键入行为解耦；括号匹配是纯扫描（无语法树，
字符串/注释内括号会误配，文档注明），大文档只扫光标前后 100KB 窗口。

## K. 导航阅读三件（当前行高亮 + 符号大纲 + 查找接线）

**原因**：编辑器“读导航”缺口——光标行无定位、Rust 大纲要手写解析、
搜索面板与编辑器的 60 行胶水每个应用重复写。
**好处**：三件全是薄封装（装饰集合/tree-sitter 现有树/面板现有回调），
`search` 演示页胶水从 ~60 行收到 1 行 `attach_editor` 即是证明。

```rust
// 1. 当前行高亮（默认开，仅多行；空行无可见高亮，装饰只盖文本区）
InputState::new(window, cx).current_line_highlight(true);
// 2. 符号大纲（需高亮器实现 document_symbols，tree-sitter 已实现）
let symbols: Vec<DocumentSymbol> = state.document_symbols(); // kind/name/range/start_row
state.goto_symbol(&symbols[0], cx); // 光标到符号头 + 滚动可见
// 3. 查找接线（一行：文本同步 + 匹配标黄 + 默认跳转；替换权仍在外部）
panel.update(cx, |panel, cx| panel.attach_editor(&text, cx));
panel.update(cx, |panel, cx| panel.set_highlight_colors(yellow(), None, cx));
panel.focus_search_input(window, cx); // Ctrl+F 接线用
```

约束：`document_symbols` 是 `Highlighter` trait 默认空实现（非 tree-sitter
后端返回空，不编译失败）；`attach_editor` 不覆盖已有的自定义 `on_navigate`，
替换回调一律外部设置（文本归属不同，框架不代劳）。

## L. 多光标多选区（`input_ui/multicursor.rs`，新建 + 渲染层向量化）

**原因**：编辑器“写”的最后一块拼图——无多光标，对齐编辑/批量改名只能靠
查找替换，VS Code 用户到这一步会直接判定“不能用”。
**好处**：模型零回归（主选区不动，既有 500+ 测试全过），编辑扇出 + 渲染
一次到位，`editor` 演示页可直接手感验证。

```rust
// 新增光标：Alt+点击、上下加光标动作（cmd/ctrl-alt-up/down，Input 上下文）
actions!(input, [AddCursorAbove, AddCursorBelow]);
// 主光标身份保持；extras 有序互不重叠（normalize_extras 维护）
state.has_multiple_cursors(); // state.extra_selections()
```

语义（对标 VS Code，v1 边界文档注明即契约）：键入/退格/删除/剪切/复制/
粘贴/回车扇出到所有光标（自后向前应用，偏移不失效）；扇出编辑并组进撤销栈
（`History::regroup_last`，一次撤销整体回退）；纯键盘移动/鼠标左键/Esc/
全选/行操作坍缩为单光标，`Shift+方向` 只扩展主选区；复制为各选区换行拼接
（全塌缩时复制各光标所在整行）；回车延续各行缩进；自动闭合/粘贴板分发/
电缩进拆行不参与扇出；掩码/单行建不了额外光标。
渲染：选区 path 向量化（主 + 额外同刷同色），额外光标与主光标同尺寸同闪烁，
滚动仍由主光标驱动；括号匹配/当前行/标黄全部跟随主光标。

## M. 收尾规划（1.2 范围：分拆收尾 + 编辑器缺口 + 快捷键用户层）

> 原"1.2 之后"转正：1.2 把编辑器"写读导航"基线补齐后（H–L），分支上还有三处没闭环——
> 半成品分拆（P3：`editor/` 平移已做、`EditorState` 外壳已落地，但路线没追认、差集没盘点）、
> LSP trait 与编辑器两张皮（`lsp/` 全套 provider 无人消费）、快捷键差文件加载最后一公里。
> 与其留到"以后"，不如 1.2 内闭环（G 章"原非目标转正"先例）。
> 落点约定（分拆后）：编辑器功能一律进 `input_ui/editor/` + `EditorState` 方法，
> 表单 `input/` 不动，共享走 `TextCore`；顺序 M1 先行（基线冻结），M2→M3→M4 串行，
> M5/M6/M7 可并行。开发计划与验收见 `1.2.0-dev-plan.md` §M；第二批转正（原非目标）见 §O。

### M1. P3 分拆收尾（文档追认 + 差集清零 + 红线冻结）

**原因**：实际路线已偏离分拆设计 §4（设计写 `EditorState{core}+Deref`，实际落地是
包 `Entity<InputState>` 的编排外壳 + 大纲缓存；编辑器实现平移 `editor/` 是 `impl InputState`，
签名没换）。四处没结算：路线没追认（文档与代码两张皮）、`EditorState` 能力差集没盘点
（旧 `CodeEditor` 用户经 `set_highlighter`/行操作/多光标等是否全有透传）、分拆设计 3.2 三件
（funnel 已下沉 P1b / overlay 已有 `editor_ui`，`extensions` 表/`on_edit` 订阅表没影）没写清、
`input/state.rs` 反弹到 3196 行（P1 曾压到 2867）。
**好处**：1.2 发版前把分拆"结算"而不是"烂尾"；M2–M6 全站在冻结后的 `EditorState` 上加功能。

```rust
// 1. 路线追认（先改分拆设计文档 §4/§5，再写代码）：P3a 包装实体路线即最终路线
// 2. 差集盘点：旧 CodeEditor mode 能力 × EditorState 透传 → 差集补透传方法，示例：
impl EditorState {
    pub fn set_highlighter(&mut self, h: Box<dyn Highlighter>, window: &mut Window, cx: &mut Context<Self>);
    pub fn document_symbols(&self) -> Vec<DocumentSymbol>;
    pub fn goto_symbol(&mut self, symbol: &DocumentSymbol, cx: &mut Context<Self>);
}
// 3. 消费方复查：combobox/sender/tag_input/otp/search_panel 等 12 处零改动目检
```

约束：3.2 三件按现状结算——已落地的留，未落地的砍（`extensions` 表/`on_edit` 订阅表不进 1.2，
分拆文档如实记录，不留"后续再做"）；`input/state.rs` 不再设行数门，改为红线"新功能禁入、只修 bug"；
每步 `cargo test -p rgpui` 全绿 + 中文 IME 手测 + 改动包 clippy。

### M2. LSP 编辑器侧接线（框架闭环，传输留应用）

**原因**：`lsp/` 下 provider trait 全套已有（`LspClient`/`CompletionProvider`/
`DiagnosticsProvider`/hover/definitions/semantic_tokens + 补全 UI/诊断 UI 状态机 +
`PositionMapping` 偏移换算），`EditorState` 零引用——trait 与编辑器两张皮，应用想接 LSP 无处挂。
**好处**：框架侧闭环：`Editor` 只认 trait，传输（stdio 进程/JSON-RPC）由应用实现 `LspClient` 注入；
补全弹窗/诊断渲染/hover 浮层一次接好，`ru_editor` 可直接消费。

```rust
impl EditorState {
    pub fn set_completion_provider(&mut self, p: Rc<dyn CompletionProvider>, cx: &mut Context<Self>);
    pub fn set_diagnostics_provider(&mut self, p: Rc<dyn DiagnosticsProvider>, cx: &mut Context<Self>);
    pub fn set_hover_provider(&mut self, p: Rc<dyn HoverProvider>, cx: &mut Context<Self>);
}
// 触发：输入经 cx.debounce（G5）→ provider → 补全弹窗（completions_ui 已有）/ 诊断进装饰集合
```

约束：只做编辑器侧（状态 + 渲染 + 防抖触发）；stdio 传输/进程管理见 §O1；
诊断渲染走 `TextDecorationCollection`（与标黄/括号匹配/当前行同池，优先级文档注明）；
`editor` 演示页接一个本地假 provider（静态词表 + 固定诊断），手工可验。

### M3. snippets 最小版（补全搭档）

**原因**：M2 补全的另一半——无占位符跳转，函数补全展开后光标只能停末尾；
LSP `completionItem`（`insertTextFormat=Snippet`）落地也靠它。
**好处**：片段表 + `$1/$2/$0` + `Tab`/`Shift-Tab` 跳转 + 补全联动，一次补齐。

```rust
// editor/snippets.rs 新建，EditorState 方法（会话状态进结构体小字段，不另起 Entity）
impl EditorState {
    pub fn expand_snippet(&mut self, template: &str, window: &mut Window, cx: &mut Context<Self>); // 解析 $1/$2/$0 + ${1:占位}
    pub fn next_placeholder(&mut self, window: &mut Window, cx: &mut Context<Self>); // Tab；Shift-Tab 上一个；Esc/提交退出会话
}
// 补全联动：选中 insertTextFormat==Snippet 的条目 → expand_snippet
```

约束：会话期间键入只改当前占位（VS Code 完整镜像编辑不做，文档注明）；
多光标下只主光标展开（其余坍缩，按 L 章"不扇出"先例）；撤销单组（`regroup_last`）；
单测：展开 + 跳转往返 + 退出 + 与补全联动（分拆红线：与 multicursor/undo/IME 交互注明）。

### M4. inlay hints（装饰薄封装，M2 之后）

**原因**：M2 之后自然接——类型/参数提示是现代编辑器标配；显示层只是行尾虚拟文本，
装饰集合现成，数据源 M2 已铺好。
**好处**：provider trait（默认空实现，不断编译）→ 装饰行 → 开关；LSP 与手动 push 两路数据源。

```rust
// editor/inlay_hints.rs 新建
pub trait InlayProvider { fn hints(&self, range: Range<usize>, ...) -> Vec<InlayHint>; /* 默认空 */ }
impl EditorState {
    pub fn set_inlay_provider(&mut self, p: Rc<dyn InlayProvider>, cx: &mut Context<Self>);
    pub fn inlay_hints_enabled(mut self, enabled: bool) -> Self; // 默认关（性能姿态保守）
}
```

约束：虚拟文本三不——不占布局、不进 `Rope`、不参与选中复制（文档注明）；
大文档只渲染可视行；无 provider 时零开销。

### M5. sticky scroll（大纲显示层，M1 之后可并行）

**原因**：大纲数据（`document_symbols`，K2）已有，只差显示层；长文件读码定位刚需，
且是纯展示、零编辑语义，最薄的一项。
**好处**：顶部粘性行 + 点击跳转（`goto_symbol` 已有），读码体验一步到位。

```rust
// editor/sticky_scroll.rs 新建；Editor 渲染顶加 overlay 层（随滚动取当前嵌套符号栈）
impl EditorState { pub fn sticky_scroll(mut self, enabled: bool) -> Self; } // 默认开，仅 Editor
```

约束：只显示大纲栈（无大纲 = 不显示，不报错）；点击走 `goto_symbol`；
高度固定一行，不挤占编辑区滚动（overlay 层，不进布局）。

### M6. 多语言注册表（机制定死，语言包不进）

**原因**：tree-sitter 现只通 Rust（`TreeSitterHighlighter::rust` 硬编码），
`supported_languages` 23 种只是 `highlight/mod.rs` 字符串表；加语言 = 加 dep + query，
逐个加是无底洞，但"怎么加"必须 1.2 定死，否则各语言实现分叉。
**好处**：注册表机制一次定：语言名 → 构造器，`EditorState` 按语言名选用；
Rust 首语言跑通；新语言三步文档（加 dep / 供 query / 注册一行）。

```rust
// highlight/mod.rs
pub type HighlighterFactory = fn() -> Box<dyn Highlighter>;
pub fn register_highlighter(language: &'static str, factory: HighlighterFactory);
pub fn highlighter_for(language: &str) -> Option<Box<dyn Highlighter>>; // 无→None，调用方降级
impl EditorState { pub fn set_language(&mut self, language: &str, window: &mut Window, cx: &mut Context<Self>); } // 查表 + set_highlighter + 大纲联动
```

约束：注册表只管构造不管 dep（各语言 grammar 仍各自 feature 门，默认全关）；
未注册语言静默降级（不 panic、不编译失败）；wasm 下 tree-sitter 整体不可用，
`set_language` 直接降级（文档注明）；Rust 之外语言见 §O6（首批 JSON + TOML）。

### M7. 快捷键用户层：文件加载 + 设置页（引擎已全，差最后一公里）

**原因**：引擎全支持（`cx.bind_keys` 后绑覆盖默认键；`NoAction`/`Unbind` 按上下文单禁，
`keymap.rs` 倒序匹配 + 上下文谓词；多键序列原生；`bindings_for_action` 取最后绑定供回显；
`secondary-*` 跨平台），`HotkeyInput`（录制）组件已有——缺的只是 keymap **文件**加载 + 设置 UI。
**好处**：`keymap.json` 解析 → `bind_keys`/`NoAction` + 设置页（录制 + 回显 + 冲突提示），
用户自定义闭环；独立无依赖，M1 后即可并行。

```rust
// keymap.rs（或 keymap/file.rs 新建）：serde_json_lenient 已是 workspace 依赖
pub fn load_keymap_json(json: &str) -> anyhow::Result<Vec<KeyBinding>>; // action 名→Action 映射表，未知 action 报错
// 设置页 recipe（文档 + 示例，不建新组件）：
//   HotkeyInput 录制 → KeyBinding { keystroke, action, context } → cx.bind_keys；
//   bindings_for_action 回显当前键；同 keystroke 不同 action 即冲突提示
```

约束：JSON 是"改键"语义（文件列 = 用户层全集，缺的走默认键，文档注明）；
未知 action 名直接报错（不静默跳过）；上下文谓词字符串原样透传（不校验，
写错匹配不上，文档注明）；文件里 `"action": null` 表禁用（= `NoAction`，只认这一种写法）。

### M 落点总表

| 组件 | 文件 | 需 Entity |
|------|------|-----------|
| P3 收尾 | 分拆设计 `1.2.0-editor-split-design.md` §4/§5 更新 + `editor/state.rs` 透传 | 否（盘点 + 透传方法） |
| LSP 接线/snippets/inlay/sticky | `editor/lsp_attach.rs`（或分文件）`editor/snippets.rs`、`editor/inlay_hints.rs`、`editor/sticky_scroll.rs` | 否（全是 `EditorState` 方法，状态进结构体小字段） |
| 多语言注册表 | `highlight/mod.rs`（注册表）+ `editor/state.rs`（`set_language`） | 否 |
| 快捷键用户层 | `keymap.rs` 或 `keymap/file.rs` + 示例页 | 否 |

已齐（1.2 内）：多光标、折叠、行操作、自动闭合、电缩进、括号匹配、
当前行、大纲、右键菜单、搜索替换、撤销、IME、软换行；
M 落地后另齐：分拆结算、LSP 接线、snippets、inlay hints、sticky scroll、多语言注册表、keymap 文件；
O 落地后另齐：stdio 传输、Minimap、Vim 最小可用、CodeLens、标尺/括号彩虹、JSON+TOML grammar、
extensions/on_edit 表。

## O. 第二批转正（原非目标，开发计划见 `1.2.0-dev-plan.md` §O）

> 转正原因统一：M1 结算时四项确实无需求（M2–M6 未落地）；现编辑器功能数已超 10 个
> （补全/诊断/hover/snippets/inlay/sticky/行操作/多光标/闭合/匹配/当前行/菜单），
> 传输/视图/扩展总线有了真实消费方，继续"不做"才是欠账。

### O1. stdio LSP 传输/进程管理（`lsp/stdio.rs` 或 `lsp/process.rs`，新建）

**原因**：M2 只到 provider 接线，应用想接 rust-analyzer 还得手写整套 stdio +
JSON-RPC，`LspClient` trait 空有定义无人实现——框架闭环差最后一公里。
**好处**：`StdioLspClient: LspClient` 开箱即用，M2 的 provider 可直接包一层真传输；
stdio 是 LSP 事实标准，TCP/websocket 明确不做，不发散。

```rust
// lsp/stdio.rs 新建
pub struct StdioLspClient { child: std::process::Child, next_id: i64, pending: HashMap<i64, oneshot::Sender<Value>> }
impl StdioLspClient {
    pub fn spawn(command: &str, args: &[&str], cx: &mut App) -> anyhow::Result<Self>;
    pub fn request(&mut self, method: &str, params: Value) -> Task<anyhow::Result<Value>>;
    pub fn notify(&mut self, method: &str, params: Value);
}
impl LspClient for StdioLspClient { ... }
```

约束：Content-Length 头收发（LSP 传输语义，不支持纯换行分隔）；id 自增；
stderr 透日志（server 崩溃可查）；进程异常退出则在途请求全失败、不守护重启
（文档注明，守护是应用层策略）；只做 stdio。

### O2. Minimap（`input_ui/editor/minimap.rs`，新建）

**原因**：长文件读码定位刚需，大纲（sticky）只解决"跳哪"，Minimap 解决"在哪"；
现在只能靠拖滚动条猜。
**好处**：缩略 + 可视区高亮 + 点击/拖动跳转，读码体验与 sticky 互补。

```rust
// input_ui/editor/minimap.rs 新建；EditorState 方法（开关进结构体小字段）
impl EditorState { pub fn set_minimap_enabled(&mut self, enabled: bool, cx: &mut Context<Self>); } // 默认关
```

约束：纯 overlay 三不（不占布局/不进 `Rope`/不参与选中，inlay 同款）；
大文档抽样渲染（每 N 行取 1，N 可配）；跳转走现有 `reveal_offset`/`goto_symbol`，
不另起滚动语义。

### O3. Vim 模式最小可用（`input_ui/vim.rs` 或 `editor/vim.rs`，新建）

**原因**：VS Code 用户到多光标那步说"能用"，到 Vim 这步说"好用"——normal/insert
高频操作无模式寸步难行；keymap 上下文谓词已支持 `vim_mode`，只差状态本身。
**好处**：三态 + 核心移动编辑一次到位，`:` 命令等重型件明确不做，不 snowball。

```rust
// 落点看表单复用情况定（只编辑器用就进 editor/）
pub enum VimMode { Normal, Insert, Visual }
impl EditorState {
    pub fn set_vim_mode_enabled(&mut self, enabled: bool, cx: &mut Context<Self>); // 默认关
    pub fn vim_mode(&self) -> VimMode; // 状态行指示用
}
// 移动 hjkl/wb/0/$/gg/G；编辑 i/a/o/x/dd/yy/p/u；Esc 回 normal
```

约束：支持矩阵文档注明（`:` 命令/宏/寄存器/插件/可视块不做，缺什么一眼可见）；
键位走 Input 上下文 action（与行操作同模式），`vim_mode` 进上下文谓词；
keymap 曾砍掉的 `vim` 命名空间预留随本节恢复。

### O4. CodeLens（`input_ui/editor/codelens.rs`，新建）

**原因**：M2 之后自然接——引用数/测试运行是"读导航"的最后一米；显示层只是
行上透镜行，数据源 M2 已铺好（diagnostics 同款注入姿势）。
**好处**：provider trait（默认空，不断编译）→ 行上透镜 → 点击回调一次闭环。

```rust
// editor/codelens.rs 新建
pub struct CodeLens { pub line: u32, pub title: SharedString }
pub trait CodeLensProvider { fn lenses(&self, ...) -> ...; /* 默认空 */ }
impl EditorState { pub fn set_codelens_provider(&mut self, p: Rc<dyn CodeLensProvider>, cx: &mut Context<Self>); }
```

约束：v1 只做单行文本透镜（多行折叠/参数透镜不做）；行高固定、不挤占编辑区滚动
（overlay 层，sticky 同款）；无 provider 零开销。

### O5. 标尺 + 括号彩虹（装饰层小件）

**原因**：两件都是"一眼"件——标尺治超长行，彩虹治深嵌套；地基全现成
（装饰集合/括号扫描），不做可惜。
**好处**：对外只有 builder 开关 + 颜色配置，与括号匹配/当前行同池。

```rust
InputState::new(window, cx).multi_line(true).rulers(vec![80, 120]); // 指定列竖线，默认关
InputState::new(window, cx).bracket_rainbow(true); // 嵌套层级按调色轮着色，默认关
```

约束：与现有括号匹配高亮共存规则文档注明（谁盖谁）；标尺只画线不参与命中；
大文档只扫可视区（括号扫描 100KB 窗口先例）。

### O6. 首批非 Rust grammar（JSON + TOML，按 M6 三步机制）

**原因**：M6 把"怎么加"定死了，但 1.2 内的语言还是只有 Rust——配置场景
（keymap.json、Cargo.toml）天天见却无高亮无大纲，注册表演了个寂寞。
**好处**：JSON + TOML 语法小、query 易写，一次验证 M6 整条链路（dep/query/注册/大纲）。

```rust
// crates/rgpui/Cargo.toml：json = ["dep:tree-sitter-json"]，toml 同理（各独立 feature 门，默认全关）
register_highlighter("json", ...); // M6 同款一行
// document_symbols：对象键 / 表头
```

约束：query 随 grammar 版本锁（Rust 内置查询同款保证）；JS/TS 等大语法不进 1.2；
wasm 下整体降级（M6 同款注明）。

### O7. 分拆 3.2 未落地件（`extensions` 表 + `on_edit` 订阅表）

**原因**：M1 砍掉时理由是"功能数少，总线过度设计"——现挂变更与扩展点的功能
已超 10 个（M2–M6 的 provider/装饰/overlay 全是各自为战的手工接线），
O1–O6 还要再加 7 个，总线有了真实需求。
**好处**：文本变更只订一处、扩展只注一处，后续功能不再重复手工胶水
（`search` 页 60 行胶水 → `attach_editor` 1 行即先例）。

```rust
// 按分拆设计 3.2 机制思想实现（原文备查，M1 结算口径）
pub fn register_extension(...); // 装饰/行为扩展注册
pub fn on_edit(...); // 文本变更订阅（window 可用性随 subscribe_in 同款约束）
```

约束：扩展走新表，不进 `input/state.rs`（M1 红线"新功能禁入"不变）；
分拆设计文档 3.2 同步更新为"已落地"；`Change` 订阅与新表并存期文档注明选用规则。
