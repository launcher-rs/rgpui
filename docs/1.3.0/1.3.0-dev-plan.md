# 1.3.0 开发文档

> 检查器（Inspector）完善 + 1.2.0 结转。每项完成后勾选。
> 库改动统一约定：中文文档注释、零 `#[allow(dead_code)]`、
> 公开 API 进 `prelude`（如适用）、改动包 clippy 干净。

## I. 检查器完善（Inspector）

> 背景：1.2.x 期间以 `examples/inspector/` 验证了检查器四件套
> （开关面板 / 面板 UI / 拾取 / 状态展示）与祖先链树脊、长链折叠、
> 节点复制路径。但仍有四块短板，本版本补齐。

- [x] I1. **树节点 ↔ 界面区域双向映射**（点击树选中画布元素并高亮）。
      实现：`Inspector::select` 公开 + `select_ancestor(levels_up)`（全局路径前缀
      反查 + hitbox 包含消歧同路径多实例）+ `Window::select_inspector_ancestor`；
      检查器打开期间常驻 `inspector_hitboxes`，选中区域橙色常驻高亮复用拾取绘制；
      示例祖先链点击即选中祖先（并复制路径）。
      待手工验收：点击树任一节点 → 画布对应区域高亮 + 面板显示该节点源码位置与布局边界。

- [x] I2. **完整元素树 + 逐节点折叠/展开**。
      实现：`Drawable::prepaint` 嵌套压栈记录 parent→children（`Frame` 存节点/根，
      面板自举暂停记录；关闭即空零开销；`is_inspector_open` 门控视图缓存禁用与
      全量 Div hitbox，保证树新鲜完整）；公开 `roots/children/parent` +
      `select_inspector_element` + `InspectorElementId::{short_label,source_label,tree_key}`；
      示例新增完整树卡片（祖先链播种展开、▸/▾ 逐节点折叠、800 行截断计数）。
      待手工验收：面板内完整树可逐节点展开/折叠（`cargo run -p inspector`）。
      注：原“量大复用 `VirtualList`”方向暂以 800 行截断计数代替
      （面板外层已滚动，嵌套虚拟列表收益低；真遇万级节点再换）；
      关闭时零开销已由门控保证（`cargo test -p rgpui --lib` 605 过）。

- [x] I3. **检查面板内置化**（默认面板，开箱即用）。
      实现：新增 `rgpui::inspector_panel`（`default_inspector_panel` +
      `render_div_inspector_state`，祖先链/完整树/Div 布局展示全搬入）；
      `App::enable_default_inspector()` 一行启用（面板 + Div 注册），
      应用入口加 `window.toggle_inspector(cx)` 共两行；
      `examples/inspector/` 822→310 行，仅保留开关演示 + 自定义横幅
      （整板替换 recipe：内部复用默认面板）。
      待手工验收：默认与自定义两种模式行为一致（`cargo run -p inspector`）。

- [x] I4. **面板自定义接口文档化**。
      落地：`docs/rgpui-book/09-inspector.md` 新增检查器章节
      （两行启用、两接口分工表、整板替换/按类型扩展 recipe、树与选中 API）；
      `examples/inspector/` 以“默认面板 + 自定义横幅”作整板替换 living recipe；
      `rgpui::render_div_inspector_state` 公开供自定义面板复用。
      Header/Section 插槽化列为可选，不阻塞发布。

## C. 回调签名统一（on_click / on_change / on_select）

> 来源：AI 升级 rgpui 实战反馈——各组件回调签名没有一致规律，
> 迁移调用点需逐个查签名。经普查确认属实，三个维度全乱：

> | 组件 | 现状签名 |
> |------|----------|
> | `Button` / `Tab::on_click` | `Fn(&ClickEvent, &mut Window, &mut App)` |
> | `Checkbox` / `Switch` / `Radio::on_click` | `Fn(&bool, …)`（引用） |
> | `RadioGroup` / `TabBar::on_click` | `Fn(&usize, …)`（引用；与 `Tab` 同名不同义） |
> | `BreadcrumbItem::on_click` | `Fn(&mut Window, &mut App)`（无事件无值） |
> | `Select::on_change` | `Fn(usize, &SharedString, …)`（值+引用混用） |
> | `Pagination` / `Rate` / `ColorPicker` / `DatePicker::on_change` | `Fn(usize / f32 / Hsla / NaiveDate, …)`（按值） |
> | `Carousel::on_change` | `Fn(usize, usize, …)`（两值） |
> | `Combobox` / `TagInput` / `Upload` | `Fn(&[…], …)`（切片引用） |
> | `Sidebar` / `Upload` / `NavigationMenu::on_select` | 值变更却叫 `on_select` |
> | 线程界限 | elements 系 `Rc`（无 `Send + Sync`），多数 components 系 `Arc + Send + Sync`，`Carousel` / `Upload` / `TagInput` 又无界限 |

- [x] C1. **统一约定并全量迁移**（breaking，按仓库约定直接改，不留兼容）：
      纯点击一律 `on_click: Fn(&ClickEvent, &mut Window, &mut App)`
     （`BreadcrumbItem` 补事件参数）；值变更一律改名 `on_change`，
      签名 `Fn(Value, &mut Window, &mut App)`，值一律按值传递
     （`bool` / `usize` / `f32` / `Hsla` / `NaiveDate` / `SharedString` / `Vec<T>`；
      现有 `&bool` / `&usize` / `&SharedString` / `&[T]` 一律改按值，
      `SharedString` / `Vec` 均为 Arc-backed 廉价克隆，回调同步执行无生命周期问题）；
      存储统一 `Arc` + `Send + Sync + 'static`（向严格方向统一，`Entity` 捕获不受影响）。
      涉及约 15 个组件
     （`button` / `checkbox` / `switch` / `radio` / `breadcrumb` / `tab_bar` /
      `select` / `pagination` / `rate` / `carousel` / `color_picker` /
      `date_picker` / `combobox` / `tag_input` / `upload` / `sidebar` /
      `navigation_menu`）及 `rgpui_story` / 全部示例 / 相关 tests 的调用点同步。
      落地注记：上表逐项对号入座（含 `Carousel` 双值保留 + `Arc` 化）；
      传导迁移库内 `dialog` / `alert_dialog` / `empty_state` /
      `notification_center` / `search_panel`（含 `ToggleButton→on_change`）；
      新增 `Context::listener_value`（按值事件版 `listener`）并迁移 4 处调用点；
      `rgpui-book/08-components.md` 回调章节已更新（含 `listener` vs `listener_value`）。
      验收：`cargo test --workspace --lib` 全过，CI 口径 clippy/check 全绿；
      `--all-targets` 下 `rgpui-dom` 旧 clone lint 为本次未动文件的预存问题，不属本项。

## F. 应用联动落地（框架侧，已完成）

> 来源：下游编辑器应用迁移实战暴露的框架缺口，本轮补齐。
> 只记录框架侧 API 与验收，不记录应用侧决策。

- [x] F1. **`TITLE_BAR_LEFT_PADDING` 公开**：标题栏左内边距由私有常量改为公开
  （macOS 80px 红绿灯位 / 非 mac 12px 不变），中文文档注释说明用途。
  验收：公开可用，下游硬编码消除。
- [x] F2. **`HotkeyInput::recording_text()`**：录制中提示语可覆盖
  （默认英文 `"Press a key..."`，多语言应用经 setter 定制）+ 行为单测。
- [x] F3. **`TreeEvent::Selected`**：树新增选中通知（仅鼠标点击触发，
  键盘移动高亮不触发，避免方向键误操作）+ 行为单测。
- [x] F4. **`StatusBar` 数据驱动重写**（breaking，dev 分支允许）：
  无实体 builder（`StatusBar::new().left(..).right(..)`，
  条目带稳定 id/图标/active/disabled/tooltip/点击），
  底色/顶边框走主题 token，无英文硬编码、无编辑器专有字段；
  `rgpui_story` 与 `v1_1_showcase` 的状态栏 demo 区同步改吃新组件。
  验收：相关用例过，两示例编译通过，clippy 干净。
- [x] F5. **`Theme::apply_named(name, window, cx)`**：按名称应用注册表主题
  （查表 → 按该主题 `mode` 切换并设为对应模式当前主题，未知名称返回 `false`）
  + 单测。验收：`theme::tests::apply_named_applies_registered_theme` 通过。
- [x] F6. **`HotkeyListInput` 多值快捷键输入**：与单值 `HotkeyInput` 同模式
  （点击录制、按键追加去重、chip 独立删除、`Escape` 取消），
  捕获结果以 `HotkeyCapture`（`Ignored`/`Cancelled`/`Appended`）区分，
  供调用方决定是否触发 `on_change` + 单测。
- [x] F7. **`PopupMenuItem::danger`**：标准菜单项危险样式（常态红色文本，
  悬停高亮/禁用优先级不变，仅 `Item` 变体生效）+ 单测。

## G. 应用实战回流（候选 scope，未承诺）

> 来源：下游编辑器迁移实战（对话框 Root 托管 / 树 / 菜单 / i18n）。
> 与 I/C/Z 不同，本节是候选池：立项前逐项确认，不默认进 1.3.0。

- [x] G1. **对话框标识与按标识关闭**：`Root::open_dialog` 返回 `DialogId`
  （单调 id，关闭不回收），`ActiveDialog` keyed 存储，
  `Root::close_dialog_by(id)` + `WindowExt::close_dialog_by`（关栈顶恢复焦点，
  关下层上层不受影响，未知 id 返回 `false`）。
  验收：`close_dialog_by_id_closes_lower_leaves_upper` 通过
  （开两框，按 id 关下层，上层不受影响；未知 id 返回 `false`）。
- [x] G2. **App 上下文回调回实体的 recipe**：`rgpui-book/08-components.md`
  落对话框 recipe（两段式：直挂走 `window.root::<V>()`，
  Root 包装穿透 `Root::view`，调用点无手写 downcast）；
  暂不加便捷 API（两段式已覆盖全部挂载形态）。
- [x] G3. **树键盘确认事件**：`TreeEvent::Confirmed(id)`（Enter 在文件行触发，
  目录行仅切换展开，与 `Selected` 仅点击触发对称）。
  验收：`test_confirm_emits_confirmed_on_file` 通过（文件行 Confirmed + 目录行折叠无 Confirmed）。
- [x] G4. **无状态 Tabs**：新增 `rgpui::tabs::{Tabs, TabsItem}`（值驱动
  `items/active/on_change`，`active` 按 id 比对，与 `StatusBar` 同模式，无实体；
  主题 token 走 `tab*` 系列，`on_change(SharedString)` 按 C1 约定）。
  验收：静态页签零同步代码接入 + `tabs_defaults_to_no_active` /
  `renders_static_tabs_without_panic` 通过。
- [x] G5. **弹出菜单构造期位置上下文**（调研结论：记 cookbook 约束，不硬上）：
  `submenu` 的 `window`/`cx` 透传必需（构造期建实体 + 接父链/优先级）；
  光标位置构造器内 `window.mouse_position()` 即取，无缺口；
  锚点 bounds 构造期布局未发生拿不到，两阶段定位要动 render 管线代价高。
  约束已记入 `rgpui-book/08-components.md` 菜单节。
- [x] G6. **i18n 管理器 Global + 语言目录**：`impl Global for I18nManager`
  （`cx.set_global` / `cx.global` 共享；多应用各设各的全局，避免过度设计）+
  `load_locale_dir`（`<locale>.json` 按文件名加载，原生平台）+
  `I18nSnapshot` 快照/回退 API（`snapshot` / `restore`，prelude 导出）。
  验收：`test_snapshot_restore` / `test_load_locale_dir` /
  `test_manager_is_global` 通过。
- [x] G7. **Dialog 遮罩可见性公开**：新增 `Dialog::overlay_visible(bool)` setter
  （文档定调：`overlay` 声明要不要，`overlay_visible` 控制本帧显不显示；
  `Root` 托管自动管理，裸挂自行置 `true` 出变暗背景）。

## Z. 1.2.0 结转

- [x] Z1. 删除 `chat_ui.rs` 的 deprecated 别名（1.2.0 A3 计划“1.3.0 删除”；
      确认内部零引用旧路径后删除）。

## 非目标

- 不做跨窗口/远程检查（ inspected 限单窗口本地）。
- 不做性能剖析（帧率已有 `FpsHud`，不属检查器范畴）。
- I2 若保留映射代价超标，降级为“祖先链 + 同级节点”展示，不硬上全树。

## 收尾验证

- `cargo check --workspace` / `cargo fmt --all` / 相关包 clippy / `cargo test --workspace`
- `examples/inspector/` 在默认面板 + 自定义面板两种模式下手工验证
  （开关、拾取、树折叠/复制、祖先选中高亮）
- AGENTS.md 完整性清单全过；CHANGELOG 追加 1.3.0 节
